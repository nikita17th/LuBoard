package com.lutalic.luboard.core.model

/**
 * Repository pattern will persist your objects and the
 * need of having to know how those objects would be
 * actually persisted in the underlying database, i.e.,
 * without having to be bothered about how the data
 * persistence happens underneath. The knowledge of
 * this persistence, i.e., the persistence logic,
 * is encapsulated inside a class called “Repository”.
 */
interface Repository